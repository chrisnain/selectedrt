<%= form_for(@retweet) do |f| %>
  <% if @retweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@retweet.errors.count, "error") %> prohibited this retweet from being saved:</h2>

      <ul>
      <% @retweet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tweetid %><br />
    <%= f.number_field :tweetid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
